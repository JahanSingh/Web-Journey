# Introducing Authentication with JWT (JSON Web Token)

When user credentials are send with HTTP request to the server. Server verifies it and send back a token when it verified to be valid back to the browser.
It is done to verify for every subsequent request to server.

